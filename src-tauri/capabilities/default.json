{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core defaults only (the harness page talks to its host over HTTP/WS, not Tauri IPC)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
